{
  "name": "trash-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root for platform backend/frontend/infra",
  "workspaces": [
    "platform/frontend"
  ],
  "scripts": {
    "check": "echo \"No root checks configured yet\"",
    "build:frontend": "npm run build --workspace @platform/frontend"
  }
}
