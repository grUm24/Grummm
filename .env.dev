# Development environment — safe to commit, no secrets
# These values are also set in docker-compose.dev.yml;
# this file serves as documentation and can be sourced by scripts.

ASPNETCORE_ENVIRONMENT=Development
POSTGRES_DB=platform_dev
POSTGRES_USER=platform_dev
POSTGRES_PASSWORD=platform_dev
